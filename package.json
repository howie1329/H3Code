{
  "name": "h3code",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "dev:cloud": "npm run dev --workspace @h3code/cloud",
    "build:cloud": "npm run build --workspace @h3code/cloud",
    "check:cloud": "npm run check --workspace @h3code/cloud",
    "dev:desktop": "npm run dev --workspace @h3code/desktop",
    "dev:desktop-zero": "npm run dev --workspace @h3code/desktop-zero",
    "build:desktop-zero": "npm run build --workspace @h3code/desktop-zero",
    "check:desktop-zero": "npm run check --workspace @h3code/desktop-zero",
    "dev:web": "npm run dev --workspace @h3code/web"
  },
  "devDependencies": {
    "thomas-agentkit": "^0.9.0-alpha.1",
    "turbo": "^2.5.0"
  }
}
