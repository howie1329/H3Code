/** Shared layout spine for the active workspace column (header, transcript, composer). */
export const WORKSPACE_COLUMN_MAX_W_CLASS = "max-w-[46rem]";
export const WORKSPACE_COLUMN_INSET_CLASS = "px-6";

/** Transcript virtual list gap (px); aligned with sidebar row rhythm. */
export const WORKSPACE_TRANSCRIPT_ITEM_GAP = 10;
