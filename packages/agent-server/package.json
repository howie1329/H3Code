{
  "name": "@h3code/agent-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "npm run build --workspace @h3code/agent-metadata && npm run build --workspace @h3code/pi-provider && tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build --workspace @h3code/agent-core && npm run build --workspace @h3code/agent-metadata && npm run build --workspace @h3code/pi-provider && tsc -p tsconfig.test.json && node --test dist-test/**/*.test.js"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.4",
    "@h3code/agent-core": "*",
    "@h3code/agent-metadata": "*",
    "@h3code/pi-provider": "*",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.0"
  }
}
